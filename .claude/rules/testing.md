# Testing Rules

- Run `npm run build` before claiming anything is "done"
- Run `npm run dev` and verify runtime behavior
- Test: happy path + error path + auth state
- For UI changes: take a screenshot and compare to the spec
- Never suppress errors to make tests pass — fix the root cause
